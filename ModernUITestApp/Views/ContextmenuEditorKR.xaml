<ContextMenu x:Class="ModernUITestApp.Views.ContextmenuEditorKR"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    
    <ContextMenu.Resources>
        <!-- MenuItem Style -->
        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontFamily" Value="Malgun Gothic"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter ContentSource="Header"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  RecognizesAccessKey="True"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFF2F4F8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Separator Style -->
        <Style TargetType="{x:Type Separator}">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Background="{TemplateBinding Background}" Height="1" SnapsToDevicePixels="True"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Editor UI Template (for use inside Popup) -->
        <ControlTemplate x:Key="EditorTemplate">
            <Border Background="White" CornerRadius="8" Padding="4" MinWidth="200">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="5" Opacity="0.15" Color="Black"/>
                </Border.Effect>
                <Grid Margin="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <TextBlock Text="메모 편집" FontSize="9" FontWeight="Bold" Foreground="#9CA3AF" Margin="2,0,0,4" FontFamily="Malgun Gothic"/>

                    <!-- Editor Box -->
                    <Border Grid.Row="1" Background="#F3F4F6" CornerRadius="6" Padding="8,6">
                        <TextBox x:Name="PART_EditorTextBox"
                                 Background="Transparent" BorderThickness="0" Padding="0" FontSize="14"
                                 Foreground="#1F2937" CaretBrush="#3B82F6" FontFamily="Malgun Gothic, Segoe UI"
                                 AcceptsReturn="True"/>
                    </Border>

                    <!-- Helper Hints -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
                         <!-- Hint: Shift + Enter -->
                        <Border Background="#E5E7EB" CornerRadius="3" Padding="4,1" Margin="0,0,4,0">
                            <TextBlock Text="Shift+Enter" Foreground="#6B7280" FontSize="9"/>
                        </Border>
                        <TextBlock Text="줄바꿈" Foreground="#9CA3AF" FontSize="9" Margin="0,0,8,0" FontFamily="Malgun Gothic" VerticalAlignment="Center"/>

                        <!-- Hint: Enter -->
                        <Border Background="#E5E7EB" CornerRadius="3" Padding="4,1" Margin="0,0,4,0">
                            <TextBlock Text="Enter" Foreground="#6B7280" FontSize="9"/>
                        </Border>
                        <TextBlock Text="저장" Foreground="#9CA3AF" FontSize="9" FontFamily="Malgun Gothic" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
        </ControlTemplate>
    </ContextMenu.Resources>

    <!-- ContextMenu Style Applied to Self -->
    <ContextMenu.Style>
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E5E5"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}"
                                MinWidth="140">
                            <Border.Effect>
                                 <DropShadowEffect BlurRadius="12" Direction="270" Opacity="0.15" ShadowDepth="4" Color="Black"/>
                            </Border.Effect>
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ContextMenu.Style>
    
</ContextMenu>

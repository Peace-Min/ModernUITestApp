# Modern Annotation UI Test App

WPF 기반의 모던 어노테이션 UI 및 글로벌 에디터 오버레이 데모 프로젝트입니다.

## 🌟 주요 기능 (Key Features)

### 1. Global Singleton Overlay
- **전역 편집기 패턴**: 모든 어노테이션이 공유하는 단 하나의 `GlobalEditorOverlay` 컨트롤을 사용합니다.
- **성능 최적화**: 개별 객체마다 UI를 생성하지 않아 리솟스를 절약합니다.
- **유연성**: 3rd Party 라이브러리(DirectX, Skia 등) 위에서도 좌표만 있으면 오버레이를 띄울 수 있습니다.

### 2. User Experience (UX)
- **Modern Design**: 그림자(Drop Shadow), 둥근 모서리, 깔끔한 타이포그래피 적용.
- **Helper Hints**: 에디터 하단에 단축키(`Shift+Enter`: 줄바꿈, `Enter`: 저장) 안내 제공.
- **Smart Positioning**: 클릭한 어노테이션 위치에 정확히 팝업이 배치됩니다.

### 3. Localization
- **다국어 지원**: 영문(`GlobalEditorOverlay`) 및 국문(`GlobalEditorOverlayKR`) 버전을 모두 포함하고 있습니다.

## 🚀 실행 방법 (How to Run)

1. **실행**: 프로젝트(`ModernUITestApp.sln`)를 열고 실행합니다.
2. **테스트 (Simulation)**:
   - 화면에 보이는 회색 박스(가상의 3rd Party 어노테이션)를 **오른쪽 클릭**합니다.
   - 컨텍스트 메뉴가 뜨면 **"내용 편집"** 또는 **"삭제"**를 누릅니다.
   - 팝업 에디터에서 텍스트를 입력하고 `Enter`를 눌러 저장합니다.
   
## 📁 구조 (Project Structure)
- `Views/GlobalEditorOverlay.xaml`: 영문 버전 오버레이
- `Views/GlobalEditorOverlayKR.xaml`: **한글 버전 오버레이** (현재 적용됨)
- `ViewModels/MainViewModel.cs`: 어노테이션 데이터 관리
